FROM node:20-slim

# Install mcp-proxy binary
RUN npm install -g mcp-proxy autotask-mcp

# Expose SSE port
EXPOSE 8080

# Run mcp-proxy and spawn autotask-mcp as stdio
CMD ["mcp-proxy", "--host=0.0.0.0", "--port=8080", "--pass-environment", "--", "autotask-mcp"]
